---
title: Настройки редактора
date: 2026-09-24
tags: [level_author]
---

# Настройки редактора

Вкладка настроек редактора, создание уровня и использование чужой работы

## Редактор уровней

Здесь настраиваются автосохранение, камера, выделение и форматы файлов

| Настройка | Что делает | По умолчанию |
|---|---|---|
| `Автосохранение` | включает автосохранение | включено |
| `Частота автосохранения` | сколько секунд проходит от несохранённой правки до автосохранения | 60 |
| `Максимальное количество автосохранений` | сколько копий хранится. При достижении предела самая старая удаляется | 25 |

Таймер идёт, только пока есть несохранённые правки

Одно автосохранение делает две вещи:
1. Пишет копию уровня в `<папка игры>/backups/<id уровня>/`
2. Сохраняет сам уровень, как `Ctrl+S`

Копия содержит только файл уровня. Восстановить её можно двумя способами:
- в настройках уровня: `Опасная зона` → `Восстановить из копии`
- вручную: скопировать её в папку уровня под именем `level.json` (или `level.blob`)

Подробнее - [[4_not-losing-work#Автосохранение]]

`Минимальный размер камеры редактора` и `Максимальный размер камеры редактора` ограничивают, насколько можно приблизить и отдалить вьюпорт редактора

`Мультивыбор требует удержания` и `Выбирать невидимое по рамке` меняют смысл клика во вьюпорте.
Проверьте их, если выделение вдруг ведёт себя не так, как вы привыкли

`Формат уровня` и `Формат ресурсов` задают формат записи на диск по умолчанию

Подробнее - [[3_speed-and-shortcuts]]

## Сетка

Сетка вьюпорта рисуется за уровнем и помогает расставлять объекты

| Настройка | Что делает | По умолчанию |
|---|---|---|
| `Сетка включена по умолчанию` | включена ли сетка при открытии редактора. Это только начальное состояние | выключено |
| `Шаг сетки` | сторона одной клетки в мировых единицах | 1 |
| `Прозрачность сетки` | насколько заметны линии, от 0 до 1 | 0.25 |

Кнопка сетки на панели инструментов включает и выключает её. Это состояние между сессиями не сохраняется

Как выглядит сетка:
- У неё нет края. Клетка меняется шагами по 10 при зуме, и более мелкая сетка проявляется постепенно
- Если уровню сетки понадобилось бы слишком много линий, этот уровень выключается, а не прореживается
- Цвет линий - всегда обратный фону камеры на текущем кадре, поэтому сетка видна и при смене фона. Задаёте вы только прозрачность

С включённым примагничиванием перетаскивание позиции прилипает к половине клетки: к пересечениям и к центрам клеток. Берётся та сетка, которую видно. С выключенной сеткой позиция прилипает к постоянному шагу

## Выделение

| Настройка | Что делает | По умолчанию |
|---|---|---|
| `Мультивыбор требует удержания` | включено: в режиме мультивыбора клик добавляет, только пока зажат `Ctrl`, а обычный клик заменяет выделение. Выключено: пока режим включён, каждый клик добавляет | включено |
| `Показывать коллайдер при выборе` | рисует хитбокс каждого выделенного объекта полупрозрачной заливкой | выключено |
| `Выбирать невидимое по рамке` | клик выбирает объект по всему его прямоугольнику, а не по тому, что он рисует | выключено |
| `Задержка долгого нажатия` | сколько длится удержание, прежде чем откроется меню, в секундах | 0.5 |
| `Смещение, отменяющее долгое нажатие` | насколько курсор может сместиться во время удержания, прежде чем это станет перетаскиванием | 8 |
| `Прозрачность хитбокса выделенного` | насколько плотно рисуется хитбокс выделенного объекта | 0.5 |
| `Прозрачность хитбоксов в общем виде` | то же для показа всех хитбоксов, бледнее, потому что там их перекрываются сотни | 0.25 |

Отпущенный `Ctrl` в режиме мультивыбора ничего не снимает и из режима не выводит. Выделение заменит только следующий обычный клик. Подробнее о режиме - [[4_hierarchy-and-clipboard]]

Кнопка коллайдеров на панели инструментов показывает хитбоксы всего, что есть в кадре, включая круг игрока. Это состояние между сессиями не сохраняется. Если хитбоксов на экране слишком много, показ сам выключается с сообщением, а не рисует только часть

Объект без коллайдера не рисует хитбокс ни в одном из режимов, как и неактивный объект

## Гизмо

| Настройка | Что делает | По умолчанию |
|---|---|---|
| `Размер ручек гизмо` | насколько крупные ручки перетаскивания во вьюпорте, от 0.1 до 10. В ручку, рассчитанную на мышь, трудно попасть пальцем | 1 |

Ручки сохраняют один размер на экране при любом зуме

## Создание уровня

`Заготовки уровня` задают, с чего начинается новый уровень: пустой или с небольшим каркасом.
Без заготовки такой каркас пришлось бы каждый раз собирать вручную

> [!caution] Внимание
> В `Параметры` задаётся то, что неудобно менять потом: длина в кадрах и частота кадров

> [!tip] Подсказка
> Списки `Формат файла "level"` и `Формат файла "metadata"` выбирают, в каком формате уровень и его метаданные пишутся на диск. Оба можно поменять потом в `Опасная зона` уровня

Подробнее - [[2_first-level]]

## Использование чужой работы

Каждый внешний ресурс в уровне (музыка, изображения, шрифты, тексты) должен подходить под один из двух вариантов:
- лицензия не менее свободная, чем `CC BY-NC`
- собственное разрешение правообладателя на публичное некоммерческое распространение

> [!caution] Внимание
> Личного "да, без проблем" недостаточно. Уровень под `CC BY-NC` распространяется публично. Разрешение должно покрывать именно это, а не только ваше личное использование

Уровень, который остаётся на вашем устройстве, не проверяется.
Правила применяются в один момент: когда уровень отдают сервису

Полные правила, список принимаемых лицензий, шаблон запроса и где искать ресурсы:
- [[2_legal-resource-paths]]
- [[6_asking-permission]]
- [[3_where-to-get-resources]]
